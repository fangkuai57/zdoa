# 压缩功率前端 DOA 论文故事框架

## 1. 当前最适合写成论文主线的结论

当前最稳、最能写成论文主线的版本不是老师思路下的反推型 `W`，而是：

- **前端**：压缩功率前端
  - `Y = W X`
  - `z = (1/L) * sum_{l=1}^L |Y(:,l)|^2`
- **窄带后端**：`on-grid` 支撑初始化 + `off-grid` 连续角度细化
- **宽带后端**：多频带共享角度支撑的联合恢复
- **波束矩阵 `W`**：常规 steer 型波束库，按场景在 `Uniform / CenterDense / EdgeDense` 中选择

这条主线已经有一组较完整、较稳定、可复现的高 `MC` 结果支撑，适合先写成 IEEE conference 初稿。

老师提出的“先设目标响应，再反推 `W`”的方向是有价值的，但以我们目前的实验结果看：

- 它**还没有稳定超过**当前主线
- 更适合作为：
  - 讨论部分中的前端联合设计动机
  - 消融/扩展实验
  - 或后续工作

因此，**论文主故事建议围绕“压缩功率前端下的窄带/宽带统一 off-grid DOA 恢复框架”来写**，而不是围绕反推型 `W` 来写。

---

## 2. 一句话论文故事

我们考虑一种**强压缩前端**：不保留阵元域完整复数据，而只保留低维波束域平均功率观测。

在这样的前端下，传统阵元域 `MUSIC / MVDR` 并不天然匹配。为此，我们提出一种：

- 面向功率观测建模的窄带/宽带统一 DOA 恢复框架
- 采用 `on-grid` 初始化与 `off-grid` 连续细化
- 宽带情形下采用共享角度支撑的多频带联合恢复

并在若干具有挑战性的 `off-grid` 场景中，验证该方法在压缩功率前端下优于多个阵元域、波束域和功率域基线。

---

## 3. 建议标题

可以考虑以下几个方向：

### 稳妥型

- **Off-grid DOA Estimation from Compressed Beam Power Measurements**
- **Power-Domain Off-grid DOA Recovery under a Compressed Beamspace Front-End**

### 更强调窄带/宽带统一

- **Unified Narrowband and Wideband Off-grid DOA Estimation from Compressed Beam Power Observations**

### 更强调功率前端建模

- **DOA Estimation with a Compressed Power Front-End: Modeling, Recovery, and Wideband Extension**

如果要先写初稿，我最推荐：

- **Unified Off-grid DOA Estimation from Compressed Beam Power Measurements**

---

## 4. 问题建模

### 4.1 前端观测模型

我们不直接使用阵元域完整复快拍，而是使用如下压缩前端：

```text
Y = W X
z = (1/L) * sum_{l=1}^L |Y(:,l)|^2
```

其中：

- `X in C^(M x L)`：阵元域快拍数据
- `W in C^(P x M)`：波束形成矩阵
- `Y in C^(P x L)`：波束域输出
- `z in R^(P x 1)`：送入后端的平均功率观测

本次主要实验设置为：

- `M = 20`
- `P = 20`
- `L = 40`

因此，前端最终只保留一个 **`20 x 1` 的实值功率向量**。

这与传统阵元域 DOA 问题本质不同，因为：

- 失去了阵元域完整复相位关系
- 只保留了平均功率
- 估计器必须直接针对功率统计模型来设计

### 4.2 窄带功率模型

在窄带情形下，对扫描角 `theta_n` 构造 steering vector `a(theta_n)`，则功率字典为：

```text
D(:, theta_n) = |W a(theta_n)|^2
```

观测模型写成：

```text
z ≈ sum_{k=1}^K p_k |W a(theta_k)|^2 + sigma^2 c
```

其中：

- `theta_k`：待估计 DOA
- `p_k >= 0`：源功率
- `c = diag(W W^H)` 对应噪声底项

### 4.3 宽带功率模型

对第 `f` 个频带：

```text
z_f = (1/L) * sum_{l=1}^L |W_f X_f(:,l)|^2
```

建立每个频带的功率字典：

```text
D_f(:, theta) = |W_f a_f(theta)|^2
```

宽带联合建模假设不同频带共享同一组 DOA：

```text
z_f ≈ sum_{k=1}^K p_k d_f(theta_k) + sigma_f^2 c_f
```

其中：

- `theta_k` 在所有频带共享
- `p_k` 采用共享系数
- `sigma_f^2` 允许随频带变化

这使得宽带问题不是简单逐频带独立求解，而是一个**共享支撑的联合恢复问题**。

---

## 5. 方法实现

### 5.1 窄带版本

主文件：

- `zdoa_offgrid_refine.m`

流程如下：

1. 构造 `on-grid` 功率字典
2. 在小源数条件下对候选支撑进行搜索，得到初始 DOA
3. 以该支撑为初值，对每个源做坐标式局部连续细化
4. 每次细化时重新用非负加权拟合更新源功率和噪声底

连续细化的核心不是简单取最近网格点，而是允许真实角度脱离扫描网格，实现真正的 `off-grid` 估计。

### 5.2 宽带版本

主文件：

- `zdoa_wideband_offgrid_refine.m`

流程如下：

1. 多频带上共享一组 `on-grid` 初始支撑
2. 对共享 DOA 做连续细化
3. 在每次细化中联合使用所有频带的功率观测更新目标函数

因此，宽带版本的核心是：

- 共享支撑
- 联合拟合
- 连续细化

### 5.3 当前 `W` 的设计

当前主线中最有效的 `W` 仍然是常规 steer 型波束矩阵，也就是每一行对应一个波束指向角的 steering beam。当前考虑了三类设计：

- `Uniform`
- `CenterDense`
- `EdgeDense`

它们的区别不是波束公式不同，而是**波束中心在角域中的分布密度不同**。

在最终主结果中：

- 窄带最优场景使用 `CenterDense`
- 宽带最优场景使用 `Uniform`

---

## 6. 对比方法

论文中已经形成比较完整的一组基线，可分为三类。

### 6.1 阵元域基线

1. `MUSIC`
2. `MVDR`

它们使用完整阵元域复数据 `X`，信息量高于我们的压缩前端，因此更像“性能上界参考”。

### 6.2 波束域复数据基线

3. `Beamspace MUSIC`
4. `Beamspace MVDR`

它们使用 `Y = W X`，保留了波束域复快拍与互相关。

### 6.3 功率域公平基线

5. `Power Bartlett`
6. `Power NNLS`
7. `Power SBL`

这三类方法与我们一样，都基于压缩后的功率观测，因此是更公平的比较对象。

### 6.4 我们的方法

8. `Our ZDOA-offgrid`

---

## 7. 当前最强主结果

### 7.1 窄带主结果

目录：

- `D:\heinu\Textbooks_Letures\SCI\paper\code\zdoa\compare_methods_offgrid_20260326_154217`

场景：

- `NB-K3 close offgrid`
- 真值 DOA：`[-6.35, -2.05, 1.75] deg`
- 功率：`[1.0, 0.9, 0.7]`
- `W design = CenterDense`

高 `MC` 汇总结果：

- `Our ZDOA-offgrid`: `0.495309 deg^2`
- `Power SBL`: `6.844762 deg^2`
- `Power NNLS`: `8.336333 deg^2`
- `Beamspace MUSIC`: `27.872824 deg^2`
- `MUSIC`: `30.189943 deg^2`
- `MVDR`: `39.755717 deg^2`

可见在该窄带三源近邻离网格场景中，我们的方法明显优于全部 7 个对比方法。

### 7.2 宽带主结果

目录：

- `D:\heinu\Textbooks_Letures\SCI\paper\code\zdoa\compare_methods_offgrid_20260326_100314`

场景：

- `WB-K2 strong-weak offgrid`
- 真值 DOA：`[-4.7, -0.7] deg`
- 功率：`[1.0, 0.33]`
- 频率比：`[0.85, 0.925, 1.0, 1.075, 1.15]`
- `W design = Uniform`

高 `MC` 汇总结果：

- `Our ZDOA-offgrid`: `0.155673 deg^2`
- `Power NNLS`: `1.999254 deg^2`
- `Power SBL`: `8.380258 deg^2`
- `MUSIC`: `22.158460 deg^2`
- `Beamspace MUSIC`: `26.198959 deg^2`
- `MVDR`: `36.760749 deg^2`

可见在该宽带一强一弱离网格双源场景中，我们的方法同样明显优于其它方法。

---

## 8. 如何讲“为什么我们会赢”

这部分是论文故事最关键的一环。建议不要简单说“我们的方法更强”，而是要把赢的原因讲清楚。

### 8.1 我们的方法和前端匹配

传统 `MUSIC/MVDR` 的优势建立在：

- 阵元域复数据
- 完整互相关信息
- 子空间结构

而我们的前端只保留：

- 波束域平均功率 `z`

因此，直接拿阵元域方法来解这个问题并不匹配。我们的方法之所以有优势，首先是因为：

- **模型与前端一致**
- **目标函数与功率统计匹配**

### 8.2 我们的方法对 off-grid 更友好

很多功率域基线停留在固定扫描网格上，或者只做简单谱匹配。我们的方法引入了：

- `on-grid` 初始化
- `off-grid` 连续细化

因此能显著降低离网格误差。

### 8.3 宽带版本利用了共享支撑

宽带情形下，不同频带的观测并不是独立处理，而是通过共享角度支撑联合恢复。

这使得：

- 单频带不够稳定的估计
- 可以在多频带联合下得到增强

这也是宽带场景中能优于单纯频带求谱相加类基线的重要原因。

### 8.4 挑战性场景对功率域建模更有利

当前两个主结果场景都不是最简单的：

- 窄带：三源近邻 + 离网格
- 宽带：一强一弱 + 近邻 + 离网格

这些场景会削弱传统子空间法的阈值稳定性，而功率域建模 + 稀疏/支撑恢复的优点更容易体现出来。

---

## 9. 老师思路与当前探索结果

老师提出的思路是：

- 先规定理想角响应
- 再反推 `W`

这条线我们已经做了几轮探索：

1. `impulse-fit W`
2. `power-fit W`
3. `local power-fit W`
4. `teacher inverse W`
5. `teacher W + codebook decoder`
6. `ROI / local beam-grid` 变体

### 当前结论

- 直接做“理想冲激响应反推 `W`”目前不稳定，通常会放大噪声和失配
- 单独替换前端、后端不变时，大多数反推型 `W` 还不如当前 steer 型 `W`
- 老师思路是**有研究价值的前后端联合设计方向**
- 但以目前数据看，它**还不适合作为本论文的主贡献**

### 论文中建议如何处理

建议作为：

- 讨论部分中的“前端联合设计动机”
- 补充实验 / 消融
- 后续工作

而不要在主结论中宣称：

- 反推型 `W` 已经优于当前主线

因为这个结论目前并不稳。

---

## 10. 推荐的论文贡献表述

当前最安全、最像论文 contribution 的写法是：

1. **提出了一个面向压缩功率前端的 DOA 建模框架**  
   在前端只保留低维平均波束功率的条件下，重新建立 DOA 恢复模型。

2. **提出了一个窄带/宽带统一的 off-grid 功率域恢复方法**  
   通过 `on-grid` 初始化和连续角度细化解决离网格问题，并在宽带下采用共享支撑联合恢复。

3. **构建了系统的多类基线比较体系**  
   同时对比阵元域、波束域和功率域基线，公平展示压缩前端下的性能差异。

4. **在挑战性 off-grid 场景中验证了方法优势**  
   包括窄带三源近邻场景和宽带一强一弱近邻场景。

---

## 11. 不建议在论文里过度宣称的内容

以下说法现在不建议写得太满：

- “普遍优于 MUSIC/MVDR”
- “在所有常规场景都最优”
- “老师思路的反推型 `W` 已经被验证优于 steer 型 `W`”

更稳妥的表述是：

- 在**压缩功率前端**下，我们的方法对若干具有挑战性的 `off-grid` 场景表现出明显优势
- 当前结果支持它作为一条有效的方法线
- 但前端 `W` 的更优联合设计仍是后续工作

---

## 12. IEEE 初稿建议结构

### Section 1. Introduction

建议包含以下四层：

1. 阵列 DOA 的经典背景
2. 当前工作与传统 DOA 的不同：我们只保留压缩功率观测
3. 现有阵元域/波束域方法对该前端不匹配
4. 本文贡献总结

### Section 2. Signal Model

写：

- 窄带前端模型
- 宽带前端模型
- 功率观测模型
- 问题定义

### Section 3. Proposed Method

分成：

1. 窄带功率域字典建模
2. on-grid 支撑初始化
3. off-grid 连续细化
4. 宽带共享支撑联合恢复

### Section 4. Experimental Setup

包含：

- 阵列参数
- `M, P, L`
- `SNR` 范围
- 扫描网格
- 对比方法
- 评价指标

### Section 5. Results and Discussion

建议按下面逻辑写：

1. 窄带结果
2. 宽带结果
3. 为什么会赢
4. 局限性

### Section 6. Conclusion

总结：

- 在压缩功率前端下建立了统一的 off-grid DOA 方法
- 窄带和宽带场景都验证了有效性
- 前端 `W` 的联合设计是下一步

---

## 13. 当前最适合插入论文的图

建议优先使用这两组主结果：

- 窄带：
  - `D:\heinu\Textbooks_Letures\SCI\paper\code\zdoa\compare_methods_offgrid_20260326_154217`
- 宽带：
  - `D:\heinu\Textbooks_Letures\SCI\paper\code\zdoa\compare_methods_offgrid_20260326_100314`

如果后面要重新出图，建议统一采用：

- 横轴：`SNR (dB)`
- 纵轴：`MSE (deg^2)` 或 `MSE (dB)`
- 风格：简洁、线型区分明显、不加右下角大块文字说明

---

## 14. 写作建议：现在可以怎么推进

如果现在就开始写 Overleaf 初稿，我建议步骤是：

1. 先写题目、摘要、引言
2. 再写系统模型
3. 把第 3 节方法部分完整写出来
4. 第 4 节实验先写统一设置
5. 第 5 节先放当前窄带/宽带主结果
6. 讨论部分最后补上老师思路的探索和局限性

最重要的是：

- **主线先写稳**
- **探索性的 `W` 联合设计不要抢主结论**

这样你会更快得到一版结构完整、逻辑闭合的 IEEE conference 初稿。
