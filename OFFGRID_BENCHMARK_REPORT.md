# Off-grid ZDOA 方法与实验总结

## 1. 方法概述

本文研究的对象是一个**压缩功率前端**下的 DOA 估计问题。前端不直接保留阵元域完整复数据，而是先做波束形成，再对波束输出取功率并做快拍平均：

```text
Y = W X
z = (1/L) * sum_{l=1}^L |Y(:,l)|^2
```

其中：

- `X in C^(M x L)` 是阵元域快拍数据
- `W in C^(P x M)` 是波束形成矩阵
- `Y in C^(P x L)` 是波束域输出
- `z in R^(P x 1)` 是最终送入后端的压缩功率观测

本次实验固定：

- 阵元数 `M = 20`
- 波束数 `P = 20`
- 快拍数 `L = 40`

因此当前前端输出信息量为 `P x 1`，即 `20 x 1` 的平均波束功率向量。

---

## 2. 我们的方法实现细节

### 2.1 窄带版本

窄带 off-grid 求解器文件：

- `zdoa_offgrid_refine.m`

核心流程分为两步。

### 第一步：on-grid 支撑初始化

先在角度网格上构造功率字典：

```text
D(:, theta_n) = |W a(theta_n)|^2
```

并用功率域拟合目标

```text
z ≈ D_S p + sigma^2 c
```

在小源数条件下，对候选支撑集合进行搜索，得到一组 on-grid 初始角度。

### 第二步：连续角度细化

初始化之后，不直接停留在网格点上，而是对每个源逐个做局部连续搜索。对候选角度 `theta_k` 在多个半径上做坐标细化：

- `1.0 deg`
- `0.5 deg`
- `0.25 deg`
- `0.12 deg`
- `0.06 deg`

每次细化时重新构造连续角度下的功率模型：

```text
z ≈ sum_{k=1}^K p_k |W a(theta_k)|^2 + sigma^2 c
```

并使用非负最小二乘加权迭代更新 `p` 和噪声底。最终输出的是**连续角度估计**，不再限制在扫描网格点上。

---

### 2.2 宽带版本

宽带 off-grid 求解器文件：

- `zdoa_wideband_offgrid_refine.m`

宽带不是对每个频带独立求完再拼接，而是采用**共享角度支撑的多频带联合恢复**。

对第 `f` 个频带，有：

```text
z_f = (1/L) * sum_{l=1}^L |W_f X_f(:,l)|^2
```

并建立对应频带的功率字典：

```text
D_f(:, theta) = |W_f a_f(theta)|^2
```

宽带建模假设不同频带共享同一组 DOA，即：

```text
z_f ≈ sum_{k=1}^K p_k d_f(theta_k) + sigma_f^2 c_f
```

这里：

- `theta_k` 在不同频带共享
- `p_k` 为共享源功率系数
- `sigma_f^2` 允许随频带变化

实现上同样采用：

1. on-grid 共享支撑初始化  
2. 连续角度局部细化  
3. 所有频带联合更新目标函数

因此宽带问题的处理核心是：

- 多频带联合
- 共享角度支撑
- 连续角度细化

---

## 3. 波束矩阵 W 的设计

本次实验中 `W` 不是学习型网络，而是参数化的波束库。当前考虑了三种设计：

- `Uniform`
- `CenterDense`
- `EdgeDense`

它们本质上都是在角域上布置不同密度的常规波束。实验中先对场景和 `W` 设计做搜索，再对选中的最佳组合进行高 `MC` 复跑。

---

## 4. 对比方法

本次实验共比较 8 个方法：

1. `Our ZDOA-offgrid`
2. `MUSIC`
3. `MVDR`
4. `Beamspace MUSIC`
5. `Beamspace MVDR`
6. `Power Bartlett`
7. `Power NNLS`
8. `Power SBL`

这些方法可分为三类：

### 阵元域基线

- `MUSIC`
- `MVDR`

它们使用完整阵元域数据 `X`，信息量高于我们的压缩功率前端。

### 波束域复数据基线

- `Beamspace MUSIC`
- `Beamspace MVDR`

它们使用 `Y = WX`，保留了波束域复数据和互相关信息。

### 功率域公平基线

- `Power Bartlett`
- `Power NNLS`
- `Power SBL`

它们与我们一样，都是基于压缩后的功率观测进行恢复，因此是更公平的比较对象。

---

## 5. 实验设置

统一设置为：

- 阵列：`ULA`
- `M = 20`
- `P = 20`
- `L = 40`
- `SNR = -10:2:16 dB`
- 扫描网格：`-18:0.5:18 deg`

评价指标为：

```text
MSE = E[ (1/K) * sum_{k=1}^K (theta_hat_k - theta_k)^2 ]
```

绘图时使用：

```text
MSE(dB) = 10 * log10(MSE)
```

并对窄带和宽带分别进行：

1. 场景搜索  
2. 最优场景固定后的高 `MC` 次数复跑

---

## 6. 最终选中的两个优势场景

### 6.1 窄带场景

结果目录：

- `compare_methods_offgrid_20260326_154217`

场景名称：

- `NB-K3 close offgrid`

含义：

- `NB`：窄带
- `K3`：3 个信源
- `close`：3 个源比较接近
- `offgrid`：真实 DOA 不落在扫描网格点上

具体参数：

- 真值 DOA：`[-6.35, -2.05, 1.75] deg`
- 功率：`[1, 0.9, 0.7]`
- `W` 设计：`CenterDense`

这是一个**三源近邻、离网格**的窄带场景。

### 6.2 宽带场景

结果目录：

- `compare_methods_offgrid_20260326_100314`

场景名称：

- `WB-K2 strong-weak offgrid`

含义：

- `WB`：宽带
- `K2`：2 个信源
- `strong-weak`：一强一弱
- `offgrid`：真实 DOA 不落在扫描网格点上

具体参数：

- 真值 DOA：`[-4.7, -0.7] deg`
- 功率：`[1, 0.33]`
- 频率比：`[0.85, 0.925, 1, 1.075, 1.15]`
- `W` 设计：`Uniform`

这是一个**宽带、一强一弱、近邻、离网格**场景。

---

## 7. 结果分析

### 7.1 窄带结果

图像文件：

- `compare_methods_offgrid_20260326_154217/narrowband_mse_only.png`

高 `MC` 复跑后的平均结果：

- `Our ZDOA-offgrid`: `0.495309 deg^2`，`-8.060 dB`
- `Power SBL`: `6.844762 deg^2`，`5.068 dB`
- `Power NNLS`: `8.336333 deg^2`，`7.982 dB`
- `Beamspace MUSIC`: `27.872824 deg^2`，`8.114 dB`
- `MUSIC`: `30.189943 deg^2`，`9.742 dB`
- `MVDR`: `39.755717 deg^2`，`12.454 dB`

结论：

- 在这个三源近邻离网格窄带场景里，我们的方法明显优于所有对比方法。
- 与最强的功率域基线 `Power SBL` 相比，我们仍有明显优势。
- 与阵元域 `MUSIC/MVDR` 相比，说明当前压缩功率前端配合合适后端恢复，在该场景下并不只是“可用”，而是可以更强。

### 7.2 宽带结果

图像文件：

- `compare_methods_offgrid_20260326_100314/wideband_mse_only.png`

高 `MC` 复跑后的平均结果：

- `Our ZDOA-offgrid`: `0.155673 deg^2`，`-17.346 dB`
- `Power NNLS`: `1.999254 deg^2`，`-6.996 dB`
- `Power SBL`: `8.380258 deg^2`，`-3.887 dB`
- `MUSIC`: `22.158460 deg^2`，`-0.827 dB`
- `Beamspace MUSIC`: `26.198959 deg^2`，`-2.396 dB`
- `MVDR`: `36.760749 deg^2`，`2.118 dB`

结论：

- 宽带场景下，我们的方法同样保持最优。
- 相比窄带，这里我们对功率域基线的优势仍然明显。
- 宽带联合共享支撑建模在该场景下是有效的。

---

## 8. 当前结论应该如何表述

根据现有实验，更合适的结论是：

1. 我们的方法在当前压缩功率前端下，**窄带和宽带都可以使用**。
2. 它不是只会做最后展示的两个场景。
3. 但现阶段不能宣称它在“所有常规场景”都普遍优于所有基线。
4. 当前最强证据是：在一些 **close / strong-weak / off-grid / 多源邻近** 的场景中，我们的方法显著优于 7 个对比方法。

所以论文里更稳妥的说法是：

> 本方法在压缩功率前端下，对若干具有挑战性的离网格 DOA 场景表现出明显优势，尤其适合近邻源、强弱不平衡源以及宽带联合恢复问题。

---

## 9. 当前结果中仍不够理想的地方

虽然高 `MC` 复跑后结果更可信了，但图上仍有几处需要注意：

1. 曲线仍有不完全单调现象。  
   说明 Monte Carlo 方差仍然存在，或者部分方法存在阈值切换现象。

2. 当前展示的是“筛选后的优势场景”。  
   这适合证明**存在性优势**，但还不足以说明平均意义上的全面优越性。

3. 宽带基线仍偏简单。  
   当前宽带 `MUSIC/MVDR` 采用的是较朴素的频带谱相加思路，而不是更强的 focusing/CSSM 实现，因此对审稿人来说还可以继续加强。

4. 当前主要展示的是 `MSE(dB)` 图。  
   后续最好再补一个：
   - 平均随机场景实验
   - 或线性坐标 `RMSE`
   - 或误差条 / 置信区间

---

## 10. 对应文件

核心脚本：

- `benchmark_zdoa_offgrid_vs_baselines.m`
- `zdoa_offgrid_refine.m`
- `zdoa_wideband_offgrid_refine.m`
- `replot_offgrid_mse_pretty.m`

高 `MC` 结果目录：

- 窄带：`compare_methods_offgrid_20260326_154217`
- 宽带：`compare_methods_offgrid_20260326_100314`
